{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Substitua esta string pela sua connection string real para o PostgreSQL online.
    // Exemplo comum (pode variar ligeiramente dependendo do provedor):
    "DefaultConnection": "Host=ep-nameless-recipe-acl7qcif-pooler.sa-east-1.aws.neon.tech;Database=neondb;Username=neondb_owner;Password=npg_w6yruYsne2BM"

    // Observações:
    // SslMode=Prefer: Tenta usar SSL, mas permite a conexão sem SSL se o servidor não o suportar.
    // SslMode=Require: Força o uso de SSL. Mais seguro, mas requer que o servidor suporte SSL e que o certificado seja confiável.
    // TrustServerCertificate=True: Ignora a validação do certificado SSL. Útil para desenvolvimento/testes, mas EVITE em produção sem um certificado válido e verificação rigorosa.
    // Para produção, você geralmente quer TrustServerCertificate=False e configurar o certificado raiz confiável.
  }
}